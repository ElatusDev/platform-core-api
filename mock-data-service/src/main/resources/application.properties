# =============================================================================
# Mock Data Service — Bootstrap Configuration
# =============================================================================
# Properties here are loaded before @PropertySource, making them available
# during early Spring Boot initialization.
# =============================================================================

# MockTenantContextConfiguration overrides the @RequestScope TenantContextHolder
# with a singleton because the mock-data pipeline runs outside the web layer.
spring.main.allow-bean-definition-overriding=true
